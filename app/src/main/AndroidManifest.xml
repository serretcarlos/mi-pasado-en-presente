<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="itesm.mx.losalacranes_mipasadoenpresente">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AgregarPersona" />
        <activity android:name=".AgregarEvento" />
        <activity android:name=".ModificaPersona" />
        <activity android:name=".ModificaEvento" />
        <activity android:name=".AgregarUsuario" />
        <activity android:name=".ModificarUsuario" />
        <activity android:name=".MenuSeccionRecuerdos"></activity>
    </application>

</manifest>